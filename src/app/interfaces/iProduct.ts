export interface iProduct {
  id: number;
  cate: string;
  productName: string;
  productImageUrl: string;
  productPriceSale: number;
  productPrice: number;
  productRating: number;
  productBuyQuantity: number;
  productViews: number;
  productDescription: string;
  release_date: Date;
  update_date: Date;
}
